export default {
  plugins: {
    // Tailwind CSS plugin — generates all utility classes
    tailwindcss: {},

    // Autoprefixer — adds vendor prefixes for browser compatibility
    // Example: adds -webkit-, -moz- prefixes automatically
    autoprefixer: {},
  },
};